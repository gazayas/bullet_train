<% if can? :read, Agreement.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :agreements]),
    label: t('agreements.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('agreements.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
